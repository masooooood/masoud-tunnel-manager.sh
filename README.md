🚀 Masoud Tunnel Manager

مدیریت حرفه‌ای تانل‌های SSH (TUN و Local Forward) برای Ubuntu 22.04
طراحی شده برای سناریوی ایران ➜ خارج (تانل مستقیم) با حداقل مصرف منابع و پایداری بالا.
اجرا فقط روی سرور ایران

## 🚀 Quick Install

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/masooooood/masoud-tunnel-manager.sh/main/masoud-tunnel-manager.sh | sed 's/\r$//;2s/.*/set -euo pipefail/')
```

---

✨ قابلیت‌ها

ساخت تانل SSH TUN (ssh -w) با systemd

ساخت Local Port Forward (-L) برای رنج پورت

نصب خودکار کلید SSH روی سرور خارج

فعال‌سازی خودکار PermitTunnel yes

راه‌اندازی Agent روی سرور خارج برای تنظیم خودکار tun0

مدیریت کامل سرویس‌ها (Start / Stop / Restart / Logs / Remove)

ساخت خودکار .pub اگر وجود نداشته باشد



🛠 نحوه استفاده
1️⃣ روی سرور ایران اجرا کن

گزینه پیشنهادی:

2) Create TUN tunnel (ssh -w)

اگر Remote Bootstrap را فعال کنی، اسکریپت خودش:

کلید SSH را روی سرور خارج نصب می‌کند

PermitTunnel yes را فعال می‌کند

سرویس SSH را ریستارت می‌کند

Agent تنظیم IP تونل را روی خارج نصب می‌کند

⚠️ لازم نیست روی سرور خارج چیزی اجرا کنی.

2️⃣ مدیریت سرویس‌ها

از منو:

5) Service Manager

می‌توانی:

لیست سرویس‌ها را ببینی

استارت / استاپ / ریستارت کنی

لاگ ببینی

حذف کنی

📡 بررسی وضعیت تانل

بعد از ساخت:

ip addr show tun0
ping 10.77.0.2
📁 مسیر فایل‌ها
/etc/masoud-tunnels/
/etc/systemd/system/masoud-*.service
/usr/local/bin/masoud-*
🔐 نکته امنیتی

کلیدها در مسیر امن /etc/masoud-tunnels/keys ذخیره می‌شوند.

systemd با محدودیت‌های امنیتی اجرا می‌شود.

دسترسی به /root/.ssh نیاز نیست.

🧠 مناسب برای

عبور از محدودیت‌های شبکه

کاهش مصرف CPU نسبت به پکت

پایداری بیشتر نسبت به SSH ساده

مدیریت حرفه‌ای چندین تانل

ساده و خودمونی : آیپی لوکال بین سرورها می گیرد و از ایران به خارج عوروارد می کنید . برای کلید ssh ابتدا گزینه 4 را بزنید مشخصات مقصد و دریافت کلید ssh . بعد گزینه 4 و ساخت تانل و آیپی لوکال . نیاز به اجرا روی سرور خارج نیست . به صورت خودکار از روی سرور ایران تغییرات روی سرور خاج اعمال میشه
